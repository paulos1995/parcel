<form method="post" action="{{ path('scan_plan_admin_delete', {'id': scan_plan.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ scan_plan.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
